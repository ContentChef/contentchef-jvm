apply plugin: 'java-library'
apply plugin: 'kotlin'

apply from: '../dependencies.gradle'

dependencies {
    implementation libraries.kotlin
    compileOnly libraries.json

    api project(':contentchef-jvm-common')

    testImplementation libraries.junit
    testImplementation libraries.mockk
    testCompileOnly libraries.json
}

ext {
    publishArtifactId = 'contentchef-jvm-callback-common'
    publishArtifactDescription = "ContentChef's JVM callback common SDK stuff - https://www.contentchef.io"
}

apply from: '../publish.gradle'

group publishGroupId
version publishVersion